name: 📊 Gerar Relatório Excel

on:
  workflow_dispatch: # Permite rodar manualmente clicando num botão

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout do código
      uses: actions/checkout@v3

    - name: Configurar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Instalar Dependências
      run: |
        python -m pip install --upgrade pip
        pip install pandas openpyxl xlsxwriter

    - name: Executar Reporter
      run: python reporter.py

    - name: Salvar e Commitar Relatório
      run: |
        git config --global user.name 'RoboDerik Reporter'
        git config --global user.email 'robot@derik.com'
        git add historico_trades.csv Relatorio_RoboDerik_V70.xlsx
        # Só commita se houver mudanças
        git commit -m "📊 Relatório Atualizado: $(date)" || echo "Sem mudanças no relatório"
        git push
        
